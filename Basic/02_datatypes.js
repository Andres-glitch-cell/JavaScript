// String (cadena de texto)
let name = "Andrés Fernández"
let alias = "Andresito"
let email = "afernandezs.iesjc@gmail.com"

// Number (número)
let age = 30
let height = 1.75
let weight = 70.5

// Boolean (booleano)
let isStudent = true
let isEmployed = false

// Undefined (indefinido)
let notAssigned
console.log(notAssigned); // Output: undefined

// Null (nulo)
let emptyValue = null;

// Symbol (símbolo)
let uniqueId = Symbol("id");

// BigInt (número entero grande)
let bigNumber = BigInt(1234567890123456789012345678901234567890);

// Object (objeto)
let person = {
    name: "Andrés Fernández",
    age: 30,
    isStudent: true
};

console.log(typeof name); // Ver el tipo de dato que es name
console.log(typeof age); // Ver el tipo de dato que es age
console.log(typeof isStudent); // Ver el tipo de dato que es isStudent